# QML Small-Task Benchmark
Compare quantum kernels (fidelity/projected) and QNNs vs tuned classical embeddings on small datasets.
## Setup
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
## References
- Qiskit Quantum Kernel tutorial
- QSVC API
- PennyLane variational classifier demo
